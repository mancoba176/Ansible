---
- name: Check Disk Space Usage
  hosts: all
  become: true
  tasks:
          - name: Disk usage from command module
            command: "{{item}}"
            with_items:
              - df -h
              - free -m
            register: item         
          - debug:
              msg: "{{item}}"
           
