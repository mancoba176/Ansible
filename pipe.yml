---
 - name: Using Pipe
   hosts: all
   become: true
   tasks:
    - name: shell command
      shell: lsblk && hostname
      register: myshell_output
    - name: copy the output to a local file
      copy:
        content: "{{ myshell_output.stdout }}"
        dest: "/home/mancobd_sa/hello.txt"
      delegate_to: 10.169.78.135
